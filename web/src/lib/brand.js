// Static product name. Used in the document title and the footer link —
// these identify the software itself, not the deployment, so they must not
// vary by config. The home page heading is the only place the operator can
// rebrand (via the server's `app_name` config value).

export const APP_NAME = 'HashHush';
